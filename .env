# Optional: load portfolio and main-slide images from Cloudinary or a JSON URL
# If not set, the site uses images from src/assets/portfolio and src/assets/main-slide.

# --- Cloudinary (recommended) ---
# 1. Sign up at https://cloudinary.com (free tier).
# 2. In Dashboard → Settings → Security, enable "Resource list" under Restricted image types.
# 3. Upload images and add tags:
#    - Tag "portfolio" → appears on the Portfolio page.
#    - Tag "main-slide" → appears in the homepage slider.
REACT_APP_CLOUDINARY_CLOUD_NAME=dbqrjxk2x
REACT_APP_PORTFOLIO_DIR=portfolio
REACT_APP_MAINSLIDE_DIR=main-slide


# --- Or: JSON URLs (e.g. Google Drive links in a JSON file) ---
# URL must return a JSON array of image URLs, e.g. ["https://...", "https://..."]
# REACT_APP_PORTFOLIO_IMAGES_URL=https://your-server.com/portfolio.json
# REACT_APP_MAINSLIDE_IMAGES_URL=https://your-server.com/main-slide.json
